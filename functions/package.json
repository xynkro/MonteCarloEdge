{
  "name": "montecarloedge-functions",
  "description": "Server-authoritative dealing + chip ledger for MonteCarloEdge multiplayer",
  "type": "module",
  "private": true,
  "main": "lib/functions/src/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,auth",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@simplewebauthn/server": "^9.0.3",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.2",
    "stripe": "^22.2.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
