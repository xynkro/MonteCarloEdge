# UI audit — 2026-06-22 (overnight autonomous run)

Multi-agent static UI/UX audit across all screens (branch `feat/native-capacitor`). 39 agents, 5
dimensions (responsive/safe-area, hierarchy/consistency, accessibility, store, table), each finding
statically verified. **29 confirmed.** ~17 fixed + visually verified in the preview; the rest deferred
(table-layout items need visual iteration across player counts; the remainder are polish/refactors).

## Fixed + verified

| # | Sev | Finding | Fix | Verified |
|---|-----|---------|-----|----------|
| 2 | **HIGH** | `var(--green)`/`var(--red)` used 13× but **never defined** — P&L colors, chart-bar fills, and destructive-button colors silently fell back to neutral/transparent. | Added `--green: var(--emerald); --red: var(--rose);` aliases. | ✅ Stats now shows green P&L, red all-time trend + `-$413.07`, red "Clear All History". |
| 1 | **HIGH** | Age gate (`position:fixed`, no scroll, no safe-area) clipped its "Under 18 — leave" button + Terms link on short/notched phones — a legally load-bearing first screen. | `overflow-y:auto` + `flex-direction:column` + `margin:auto` card + safe-area padding. | ✅ 375×667: all controls visible, scrolls when shorter. |
| 3 | **HIGH** | `--faint` (#5c6878) failed WCAG AA on the legal/footer/age-gate copy. | Lightened to `#7c8a9c` (≈4.6:1). | ✅ Age-gate fine print readable. |
| 4,5,6 | MED | `.cog-sheet`, `.chat-drawer`, `.lobby-wrap` bottom CTAs sat under the home indicator (no `env(safe-area-inset-bottom)`). | Added bottom safe-area to all three (same pattern as the session's island work). | build |
| 7 | MED | No `:focus-visible` anywhere — keyboard/switch users got no focus indicator. | Global `:focus-visible` ring (azure) on interactive elements (keyboard-only, never on tap). | build |
| 8 | MED | `rgba(255,255,255,.4/.45)` labels below AA (`.cog-label`, `.hh-ago`, `.nm-src`, `.rr-wait`). | Bumped to `.6` (≈7:1). | build |
| 9 | MED | Icon-only ✕ buttons lacked accessible names. | `aria-label` on cog/chat/rebuy/inbox-delete ✕ (fomo + intro already had them). | build |
| 11 | MED | **Store busy spinner was global** (my session): one tap spun ALL Edge tiers + Restore. | Added `S.net.busyId` — spinner only on the tapped button; others `disabled` + dimmed. | build |
| 16 | LOW | Leak-Report button used ad-hoc `#7c3aed/#5b21b6` vs the `--violet` token family. | → `var(--violet)/var(--violet-2)`. | ✅ violet Leak Report. |
| 21 | LOW | A few infinite loops not suppressed under reduced-motion. | Added `.mce-shimmer`, `.rec-src.src-solving`, `.wr-trophy`. | build |
| 22 | LOW | `.tag-nit` white-on-#7d8aa0 = 3.49:1 (sub-AA). | Darkened bg to `#4a5568` (≈6.4:1). | build |
| 23 | LOW | `.et-buy` width jumped when "Get"→spinner, shifting the row. | `min-width:64px; text-align:center` + `disabled` opacity. | build |
| 25 | LOW | Restore vs tier spinner inconsistency. | Resolved by #11 (only one spinner shows at a time now). | — |
| 26 | LOW | Mixed live/Soon state on the Store looked like a bug. | Added a note when packs are "Soon" but Edge Pass is live. | build |

## Deferred (documented for prioritization)

**Need visual iteration — risky to fix blind (do at a real table):**
- **[12] MED** — seat chips may overlap/spill at 9–10 players on small phones. The fix (responsive
  seat-scale var + smaller ring radius for n≥8) must be verified across 2→10 players so it doesn't
  break low-seat tables. *Top remaining UI item.*
- **[13] MED** — trainer showdown reveal cards (42×58) may collide with neighbour seats in multiway
  pots; apply the net path's crowded-table shrink to the trainer table, verified visually.
- **[27] LOW** — edge-banner copy box (max-width 64%) may crowd the HUD art on ≤380px.

**Polish / refactors (safe but lower value):**
- [10] switch rows: `aria-labelledby` + raise small seg/inbox controls to a 44px tap target.
- [14] `.start-btn` inline-bg overrides (6×) → modifier classes (`.violet/.gold/.google`).
- [15] migrate literal border-radii to `--radius-*` tokens.
- [17] header `style="width:54px"` spacer (8×) → a `.doc-top-spacer` class / absolute back-button.
- [18] give `.hdr-btn/.result-btn/.quick-btn/.numpad-btn` the `--display` font for button consistency.
- [19] associate `<label>`s with inputs via `for=`; [20] aria-label the setup numpad value buttons.
- [24] chip-pack grid → `repeat(auto-fill, minmax(140px,1fr))` so wide screens pack 3-across.
- [28] dead `.bet-slider` CSS vs the numpad bet UI — decide one path (delete or wire) before shipping.
- [29] ensure a folded seat never shows the acting-seat spotlight on a stale frame.

## Verification
- Web build clean; engine/stress gates unaffected (no engine changes).
- Visually verified in preview @375×667: age gate (fits/scrolls, all controls), stats (green P&L +
  red trend + red destructive + violet button), home boots clean.
